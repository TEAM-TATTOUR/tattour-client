import Result from '../../../common/Result';

const Submitted = () => {
  return (
    <Result
      mainText={'신청서 접수가 완료 되었어요'}
      description={'2일 이내로 문자로 확정 도안을 전송 드려요'}
    />
  );
};

export default Submitted;
